apiVersion: v1
kind: Service
metadata:
  name: {{ include "text-editor-backend.fullname" . }}
  labels:
    {{- include "text-editor-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "text-editor-backend.selectorLabels" . | nindent 4 }}
