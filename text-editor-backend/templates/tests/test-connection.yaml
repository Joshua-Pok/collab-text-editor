apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "text-editor-backend.fullname" . }}-test-connection"
  labels:
    {{- include "text-editor-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "text-editor-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
